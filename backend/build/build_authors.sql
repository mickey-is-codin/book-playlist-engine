CREATE TABLE BUILD_AUTHORS AS
    WITH A AS (
        SELECT
            CAST(AUTHOR_ID AS INT) AS AUTHOR_ID,
            'ROLE' AS AUTHOR_ROLE,
            BOOK_ID
        FROM IMPORT_AUTHORS
    )
    SELECT *
    FROM IMPORT_BOOK_AUTHORS
    LEFT JOIN IMPORT_AUTHORS
    USING(AUTHOR_ID);